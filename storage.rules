rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // All item thumbnails and step images — public read, no client write
    // Uploads are server-side only (Firebase Admin SDK bypasses these rules)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
